@page
@model National_Healthcare_System.Pages.DoctorSchedule.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].Doctor_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].Day)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].Start_Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].End_Time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].Organization_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor_Schedule[0].Chamber_Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Doctor_Schedule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Day)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Start_Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.End_Time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Organization_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Chamber_Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Schedule_Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Schedule_Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Schedule_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
